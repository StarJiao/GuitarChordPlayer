# Config & Design Guide

本应用是**单文件**的吉他和弦转换练习器，全部功能、样式与配置都集中在：

```
player/index.html
```

需要调整玩法时，打开该文件，编辑顶部的「配置区」（搜索 `===== 配置区 =====` 即可定位）。**改完保存后刷新浏览器即可生效**，无需构建、无需联网。

---

## 一、配置区速查

| 常量 | 作用 | 默认值 |
| --- | --- | --- |
| `CHORDS` | 和弦库（可增删，界面自动更新） | 22 个常用和弦 |
| `STRUM_PATTERNS` | 扫弦节奏（两种全下，设置面板可选） | 四分全下 / 二分全下 |
| `STRUM_DEFAULT` | 默认使用哪个节奏 | `'quarter'`（四分全下） |
| `BPM_MIN` / `BPM_MAX` / `BPM_STEP` | 速度调节范围与步长 | 20 / 200 / 5 |
| `FADE_IN` | 画面淡入占每和弦时长的比例（也是扫弦开始的反应窗口） | `0.25` |
| `FADE_OUT` | 画面淡出占每和弦时长的比例 | `0`（无淡出） |
| `OPEN_FREQS` | 六根空弦的标准音频率（合成兜底用） | E2–E4 |
| `--xxx`（CSS 变量） | 主题配色与字体 | 暖棕琥珀风格 |

---

## 二、和弦库：增删和弦

`CHORDS` 数组中每个元素是一个和弦，格式如下：

```js
{ name:'C',   frets:[-1, 3, 2, 0, 1, 0], fingers:[0, 3, 2, 0, 1, 0] }
{ name:'F',   frets:[ 1, 3, 3, 2, 1, 1], fingers:[1, 3, 4, 2, 1, 1], barre:{fret:1, from:0, to:5} }
```

| 字段 | 含义 |
| --- | --- |
| `name` | 和弦名，显示在卡片与播放界面，必须唯一 |
| `frets` | 六根弦按的品格，**从低音 E 弦到高音 e 弦**：`-1` 闷音（显示 X）、`0` 空弦、`>0` 品格数 |
| `fingers` | 每根弦对应的按弦手指（1–4，`0` 不按），仅用于指法图标注 |
| `barre` | 横按（可选）：`{fret: 品数, from: 起始弦序号, to: 结束弦序号}` |

**增加一个和弦**：在数组末尾追加一行即可，例如：

```js
{ name:'Am7', frets:[-1, 0, 2, 0, 1, 0], fingers:[0, 0, 2, 0, 1, 0] }
```

**休止占位 ⌀**：`REST_CHORD` 是内置的休止符号，练歌时用于休止小节。它的 `name` 固定为 `⌀`，`rest:true` 标记其不发声（预览模式下）。不需要改。

---

## 三、扫弦节奏：`STRUM_PATTERNS`（两种全下）

设置面板「扫弦节奏」可选两种节奏，全部为**下扫**（无上扫），每和弦 4 拍：

```js
const STRUM_PATTERNS = {
  quarter: { name:'四分全下', beat:4, pattern:[
    { at:0, dir:'down' }, { at:1, dir:'down' }, { at:2, dir:'down' }, { at:3, dir:'down' }
  ]},
  half: { name:'二分全下', beat:4, pattern:[
    { at:0, dir:'down' }, { at:2, dir:'down' }
  ]}
};
const STRUM_DEFAULT = 'quarter';    /* 默认节奏 id */
```

| 节奏 | 乐谱（每和弦 4 拍） | 拍点 `at` |
| --- | --- | --- |
| 四分全下 | 下 · 下 · 下 · 下 | 0、1、2、3 |
| 二分全下 | 下 · · · 下 | 0、2（每两拍一下） |

### 字段说明

| 字段 | 含义 |
| --- | --- |
| `beat` | 每个和弦停留几拍（`4` 即 4/4 拍；想改成 3/4 华尔兹感就写 `3`） |
| `pattern[].at` | 这个动作从**第几拍**开始扫。第 N 拍写 `N`，半拍（八分音符）写 `N.5`，从 `0` 起 |
| `pattern[].dir` | 扫弦方向，两种全下节奏均为 `'down'` |

### 如何自定义

- **想换默认**：改 `STRUM_DEFAULT` 为 `'half'`。
- **想调下扫次数**：增删 `pattern` 里的条目、调整 `at`（例如想改成「下 · 下 · · 下」就写 `0、2、3`）。规则：**只写要扫的动作，空拍跳过；`at` 不要超过 `beat`**。
- **想加一种节奏**：照格式在 `STRUM_PATTERNS` 里追加一项（id 任意），保存刷新后设置面板下拉自动出现。

> 提示：播放时界面顶部会实时显示当前的扫弦方向序列（四分全下为 ↓ ↓ ↓ ↓），切换即可核对。

---

## 四、节拍与速度

| 常量 | 说明 | 默认 |
| --- | --- | --- |
| `STRUM_PATTERNS[].beat` | 每个节奏的和弦停留拍数（`4` 为 4/4 拍；设 `2` 恢复二分音符一和弦，`3` 是 3/4 拍华尔兹感） | `4` |
| `BPM_MIN` | 界面速度调节下限 | `20` |
| `BPM_MAX` | 上限 | `200` |
| `BPM_STEP` | 每次点击 ±几 BPM | `5` |
| `FADE_IN` | 淡入占每和弦时长的比例，**也是扫弦开始前的延迟**（留给反应窗口） | `0.25` |
| `FADE_OUT` | 淡出比例，`0` 表示无淡出、中间保持时间最长 | `0` |

每和弦实际时长 = `当前节奏 beat × 60000 ÷ BPM` 毫秒（节奏随选择联动）。

---

## 五、功能设计细节

### 1. 播放序列与随机洗牌

- 选中多个和弦后，点「开始」会生成播放序列 `seq`。
- **按顺序模式**：严格按选中顺序循环播放。
- **随机模式**（设置面板打开「随机」）：用洗牌算法打乱顺序，**一轮内不重复**；一轮播完重新洗牌，每轮都是新顺序。
- 随机模式有个细节：本轮最后一个和弦时，程序会**提前洗好下一轮**（`nextSeq`），保证「下一和弦预告」在换轮瞬间依然准确——预告什么就真的播什么。

### 2. 下一和弦预告与临近高亮

- 「下一和弦」开关打开时，播放界面会显示 `下一个 X`。
- 每个和弦的**最后 1 拍**，预告会进入「imminent」高亮态：琥珀底色 + 脉冲动画，提醒你准备换和弦。
- 预告与高亮在**按顺序和随机模式下都生效**（可开关）。

### 3. 预览模式 vs 节拍器

- 设置面板「预览和弦」开：播真实吉他采样扫弦（或合成兜底音），休止占位 ⌀ 不发声。
- 「预览和弦」关：变为节拍器声，每个扫弦动作响一声「咔嚓」，**第一拍带重音**，休止占位也响（方便数拍）。

### 4. 暂停与继续

- 播放中按 `空格` / 点击画面暂停，再按继续。
- 恢复时会从暂停点接着排程扫弦，**已经响过的动作不会重播**。

### 5. 发声机制

- 有真实采样（内嵌录音）的和弦播采样；没有采样的和弦用**合成兜底**：按六根弦的音高近实时顺序弹拨。
- 扫弦方向影响合成兜底的弹拨顺序：`down` 从低音弦到高音弦，`up` 从高音弦到低音弦。
- 声音统一延迟到画面淡入完成后再响起（`FADE_IN`），保留反应窗口；用短定时器近实时触发，避免手机浏览器丢弃预排的音频事件。

### 6. 快捷键

| 按键 | 作用 |
| --- | --- |
| `空格` | 开始 / 暂停 / 继续 |
| `回车` | 开始 |
| `Esc` | 停止 / 退出 |

---

## 六、外观主题

配色与字体在 `index.html` 顶部的 `:root` 中定义，常用变量：

| 变量 | 作用 |
| --- | --- |
| `--accent` | 主题强调色（琥珀），和弦名、按钮、高亮都用它 |
| `--bg` / `--bg2` / `--surface` | 背景层级 |
| `--text` / `--text2` / `--text3` | 文字层级 |
| `--display` / `--body` | 显示字体（衬线）/ 正文无衬线字体 |

改一处 `--accent` 即可整体换主题色。

---

## 常见问题

**改了配置没生效？**
检查是否保存了 `index.html`，并**强制刷新**（Mac 上 `Cmd+Shift+R`）。单文件应用无缓存依赖，刷新即生效。

**扫弦听上去没跟上节奏？**
先确认 BPM 合理；扫弦动作按 `at × 拍长` 触发，`FADE_IN` 会让声音整体后移一点，这是刻意的反应窗口。
