# 配置与设计说明

这是一个**可离线打开**的吉他和弦转换练习器。页面、样式、音频采样和配置分别位于 `player/index.html`、`player/style.css`、`player/app.js`；保存后刷新浏览器即生效，无需构建或联网。

## 配置速查

| 常量 | 用途 | 默认值 |
| --- | --- | --- |
| `CHORDS` | 和弦库 | 22 个常用和弦 |
| `BPM_MIN` / `BPM_MAX` / `BPM_STEP` | BPM 范围与每次调节步长 | 20 / 200 / 5 |
| `ROW_LEN` | 播放区每行展示的和弦数 | 6 |
| `OPEN_FREQS` | 合成音兜底用的六根空弦频率 | E2–E4 |
| `CHORD_SAMPLES` | 内嵌真实吉他扫弦采样 | 部分常用和弦 |
| `--xxx` CSS 变量 | 主题颜色和字体 | 暖棕琥珀主题 |

## 和弦库

在 `CHORDS` 数组中增删和弦，界面会自动更新：

```js
{ name:'C', frets:[-1, 3, 2, 0, 1, 0], fingers:[0, 3, 2, 0, 1, 0] }
{ name:'F', frets:[ 1, 3, 3, 2, 1, 1], fingers:[1, 3, 4, 2, 1, 1], barre:{fret:1, from:0, to:5} }
```

- `name`：界面显示的和弦名，必须唯一。
- `frets`：从低音 E 弦到高音 e 弦；`-1` 是闷音、`0` 是空弦、正数是品格。
- `fingers`：对应弦的按弦手指，`0` 表示不按。
- `barre`：可选横按，格式为 `{fret, from, to}`。

内置的 `⌀` 是休止占位。预览和弦开启时它不发声；关闭预览和弦后，它仍会跟随节拍器提示拍点。

## 节拍与声音

“拍数”控制每个和弦停留 1–16 拍，实际时长为：

```text
拍数 × 60000 ÷ BPM（毫秒）
```

播放中调整拍数会明确显示“下一和弦将改为 N 拍”；当前和弦仍按原拍数完成，切换后节拍点会同步更新。点击开始后会立刻进入第一个和弦；每个和弦的第一拍都会更强调，播放区下方的圆点会跟随当前拍移动。

“预览和弦”开启时，已有采样的和弦播放真实扫弦录音，其他和弦使用合成音兜底；关闭时改用节拍器。当前版本固定为**每拍一次下扫**，扫弦次数由“拍数”决定。

> 单文件版将采样和收款码内嵌在 HTML 中，适合直接下载离线使用；代价是首次打开需要传输完整文件。音频仅在用户开始练习后才解码，并且同一采样只会并发解码一次。

## 序列、多选与随机

- 默认模式：按选中顺序循环播放。
- 多选关闭：同一和弦最多保留一次，点击会选中或取消。
- 多选开启：每点击一次就追加一次，可编出实际歌曲的和弦走向；底部序列点击可删除指定位置。
- 随机模式：每轮打乱当前已选序列；如果你重复加入同一个和弦，它仍会在该轮中按加入次数出现。这比“每轮不重复”更准确。

播放中修改选择或随机开关时，当前和弦不被打断，后续行会立刻按新设置重排。

## 操作方式

| 操作 | 效果 |
| --- | --- |
| 空格（未聚焦控件时） | 开始、暂停或继续 |
| 回车（未聚焦控件时） | 开始；播放中结束练习 |
| Esc | 关闭弹层；或停止练习 |
| 和弦卡片的回车 / 空格 | 选中或取消该和弦 |
| 移动端竖屏播放中 | 可继续打开选择和弦、设置，修改会热加载到后续练习 |
| 移动端横屏 | 点击即进入沉浸布局并自动开始练习（暂停中则继续）；隐藏选择和弦、暂停、设置和右上角横屏按钮，等比放大和弦与节拍区；底部“×”用于退出 |

快捷键不会抢走已经聚焦的按钮、开关或链接的正常键盘操作。

## 移动端与沉浸式练习

宽度不大于 1180px 时，和弦与设置改为底部弹层，避免三栏布局压缩六个和弦图。弹层支持点击背景、Esc 关闭，并把键盘焦点限制在当前弹层内。

移动端加载后直接展示两行目标和弦，“横屏”按钮放在播放序列上方并靠右对齐，不必先开始播放。
竖屏播放时仍保留“选择和弦”和“设置”，因为这些修改会热加载到后续和弦，不会打断当前练习。
点击“横屏”进入沉浸布局时自动开始练习；若此前已暂停，则从暂停点继续。
进入横屏后隐藏右上角横屏按钮和三个主按钮，和弦卡片与节拍提示按短边等比放大，底部居中保留“×”；点击后退出网页横屏，播放中同时暂停。
设备本身已经横放时，页面会尝试请求恢复竖屏；浏览器不允许时则保持暂停，并提示用户旋转设备。

## 外观主题

主题变量都在 `:root` 中。最常改的是：

- `--accent`：琥珀色强调色。
- `--bg` / `--bg2` / `--surface`：背景层级。
- `--text` / `--text2` / `--text3`：文字层级。
- `--display` / `--body`：显示字体和正文字体。

## 常见问题

**第一次开始时真实扫弦稍慢？** 首次使用的采样需要由浏览器解码；之后同一和弦会直接使用内存缓存。